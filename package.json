{
  "name": "psk",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "description": "Scripts behind the psk Claude Code plugin. No runtime dependencies: plain Node, tested with node:test.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test \"scripts/**/*.test.mjs\""
  }
}
